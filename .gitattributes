# Normalize to LF in the repo; check out as-is. Keeps PHP/JS/CSS line endings
# stable regardless of the OS the plugin is edited on.
* text=auto eol=lf

*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.pdf binary
*.zip binary
